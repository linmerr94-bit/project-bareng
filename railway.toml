[build]
command = "pip install -r requirements.txt"

[deploy]
startCommand = "gunicorn core_system.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
healthcheckPath = "/health/"
healthcheckTimeout = 100
